/*============================================================================
  HEADER | header.liquid
==============================================================================*/

.global-header {
  padding: 0;
  width: 100%;
  z-index: 15;
  position: fixed;
  background-color: white;   

  @include at-query ($max, $medium) {
		position: fixed;
  }

  .grid--table {
    display: table;
    table-layout: fixed;
    width: 100%;

    > .grid__item {
      float: none;
      display: table-cell;
      vertical-align: middle;
    }
  }
}

/*================= Desktop Header | ===================*/

/* Store Messages */

#store-messages {
  background: #000000;

	.message {
  		position: relative;
  		padding: 15px 0 15px;
  		text-align: center;
	}	
		
  	.message .free {
  		line-height:1.3em;
      	color: #ffffff;
  		text-transform: uppercase;
  		margin: 0px;
    }
    .message .search-icon {
        display: inline-block;
        left: 0;
    	position: absolute;
    	top: 13px;
      	padding-left: 50px;
	}
  	.message .search-icon a{
      	color: white;
    }
  	.message .cart-right {
        display: inline-block;
        right: 0;
    	position: absolute;
    	top: 13px;
      	padding-right: 50px;
    }  
    .message .cart-right a{  
      	color: white;
	}
  	.message p{
      	margin: 0px;
    }
      
  @include at-query ($max, $medium) {
    display: none;
	}
}

/* Desktop Nav */

.desktop-header {
  background-color: $colorNav;
}

/* Desktop Nav */

.desktop-nav {
  @include clearfix();
  margin: 0 auto;
  width: 100%;
  height: 100px;

  @include at-query ($min, $small) {
    padding: 0px;
  }
  @include at-query ($max, $medium){
    width: 100%;
  }
}

/* Desktop Nav Logo */

.desktop-nav--logo {
  max-width: 100px;
  height: 100px;
  text-align: center;
  margin-left: 10px;
  margin-top: 10px;
  
  @include at-query ($max, $medium) {
    display: none;
  }
  
  a,
  a:hover,
  a:focus {
    text-decoration: none;
  }

  a, img {
    display: block;
    position: absolute;
    z-index: 10;
  }

  img {
 	 max-width: 100px;
  }
  @include at-query ($max, $medium) {
     display: none;
  }
}

/* Desktop Nav Bar */

.desktop-nav--holder {
  visibility: visible;
  width: 100%;
  margin: 0;
  padding-left: 135px;
  position: relative;
  top: -70px;
  width: 100%;

}

.desktop-nav--bar {
  font-size: em(18px);
  cursor: default;
  text-align: left;


  li {
    margin: 0;
    text-transform: uppercase;
    padding-bottom: 10px;
  }

  > li {
    display: inline-block;
    text-align: left;    
  }
}

/* Desktop Dropdowns */

.desktop-dropdown--columns {
  display: none;
  position: absolute;
  margin: 0px 0 0;
  width: 100%;
  padding: 20px 20px 0px 175px;
  z-index: $zindexNavDropdowns;
  background: white;

  .supports-no-touch .desktop-nav--list:hover &,
  .desktop-nav--list.desktop-nav--bar-hover &,
  .desktop-nav--bar-focus + & {
    display: block;
  }

  a {
    background-color: $colorNav;

    &:hover,
    &:active,
    &:focus {
    }
  }

}
.desktop-nav--list-link {
  display: block;
  text-decoration: none;
  padding: 15px 25px 25px 25px;
  white-space: nowrap;
  color: $colorNavText;

  &:hover,
  &:active,
  &:focus {
    border-bottom: 2px solid black;
  }
  border-bottom: 0px solid black;
  /* ^- the important bit. RGB 192,192,192 is the same as #C0C0C0*/
  
  -webkit-transition: border 250ms ease;
  -moz-transition: border 250ms ease;
  -ms-transition: border 250ms ease;
  -o-transition: border 250ms ease;
  transition: border 250ms ease; 
}

.desktop-dropdown {
  position: absolute;
  width: 100%;
  left: 0;
}

.desktop-dropdown--columns-list {
  float: left;
  width: auto;
  font-size: 17px;
  text-transform: uppercase;
  padding-right: 50px
}

.desktop-dropdown--child {
}

li.desktop-dropdown--child-list {
  font-size: 14px;
  letter-spacing: 1px;
  font-family: HelveticaNeueLTStd-Cn;
  padding-top: 10px;
  padding-bottom: 0px;
}

li.desktop-dropdown--child-list-special {
  padding-top: 10px;
  padding-bottom: 0px;
}

.site-header__cart-toggle {
  display: inline-block;
}

.site-header__search {
  display: inline-block;
  max-width: 400px;
  margin-top: $gutter / 2;
  width: 100%;
}

/*======================= Mobile Header | =================================*/

.mobile-header {
  border-bottom: 2px solid black;
  background-color: white;   
}

.mobile-header__logo-link { 
  @include at-query ($max, $medium) {
  	margin: 0 auto;
  	padding-right: 30px;
  	height: 52px;
  	padding-top: 14px;
 
  }
  img {
  	display: block;
  	max-width: 52px;
  	margin: auto;
  
  }
  @include at-query ($min, $medium) {
    display: none;
  }
}

.burger-left {
  padding: 0px !important;
}

/* Mobile Nav */

.mobile-nav {
  // Negative of .drawer left/right padding for full-width link tap area
  margin: 0;
  text-transform: uppercase;
  li {
    margin-bottom: 0;
  }
}

.mobile-nav__search {
  padding: $gutter / 2;

  .search-bar {
    margin-bottom: 0;
  }
}

.mobile-nav__item {
  position: relative;
  display: block;

  // Background color on top level items so there is no
  // element overlap on subnav's CSS toggle animation
  .mobile-nav > & {
    background-color: $colorDrawers;
  }

  &:after {
    content: '';
    position: absolute;
    bottom: 0;
    left: $gutter / 2;
    right: $gutter / 2;
    border-bottom: 1px solid $colorDrawerBorder;
  }
}

// Login/logout links can't have a class on them, so style <a> element
.mobile-nav__item a {
  display: block;
}

.mobile-nav__item a,
.mobile-nav__toggle button {
  color: $colorDrawerText;
  padding: $gutter / 2;
  text-decoration: none;

  &:hover,
  &:active,
  &:focus {
  }

  &:active,
  &:focus {
  }
}

.mobile-nav__item--active {
  font-weight: bold;
}

.mobile-nav__has-sublist {
  display: table;
  width: 100%;
  margin: 0px;

  .mobile-nav__link {
    display: table-cell;
    vertical-align: middle;
    width: 100%;
  }
}

.mobile-nav__toggle {
  display: table-cell;
  vertical-align: middle;
  width: 1%;
}

.mobile-nav__toggle-open {
  .mobile-nav--expanded & {
    display: none;
  }
}

.mobile-nav__toggle-close {
  display: none;

  .mobile-nav--expanded & {
    display: block;
  }
}

.mobile-nav__sublist {
  margin: 0;
  max-height: 0;
  visibility: hidden;
  overflow: hidden;
  transition: all 300ms cubic-bezier(0.57, 0.06, 0.05, 0.95);
  @include backface();

  .mobile-nav--expanded + & {
    visibility: visible;
    max-height: 900px;
    transition: all 700ms cubic-bezier(0.57, 0.06, 0.05, 0.95);
  }

  .mobile-nav__item:after {
    top: 0;
    bottom: auto;
  }

  .mobile-nav__link {
    font-weight: normal;
    font-family: HelveticaNeueLTStd-Cn;
  }
}

/* Level 2 Custom CSS */

.mobile-nav__sublist-1 {
  margin: 0;
  max-height: 0;
  visibility: hidden;
  overflow: hidden;
  transition: all 300ms cubic-bezier(0.57, 0.06, 0.05, 0.95);
  @include backface();

  .mobile-nav--expanded + & {
    visibility: visible;
    max-height: 700px;
    transition: all 700ms cubic-bezier(0.57, 0.06, 0.05, 0.95);
  }

  .mobile-nav__item:after {
    top: 0;
    bottom: auto;
  }

  .mobile-nav__link {
    font-weight: normal;
    font-family: HelveticaNeueLTStd-Cn;
  }
}

/* Mobile Nav Search Bar */

.mobile-nav--search-bar {
  margin: 0;
  padding: 10px 5px;
  width: 100%;
  
    .input-group-field {
      border: none;
      padding-left: 10px;
    }
}

/*============================================================================
  FOOTER | footer.liquid
==============================================================================*/

.site-footer {
  background-color: $colorFooterBg;
  color: $colorFooterText;
}

.footer-menu { 
  padding: 0 20px;
}

.footer-logo {
  max-width: 120px;
  margin: 0 auto 10px;
}

.footer-nav--column {
  display: inline-block;
  padding-right: 70px;
  font-size: 20px;
  text-transform: uppercase;
  float: left;
  padding-top: 88px;
  
    @include at-query ($max, $medium) {
	  text-align: center;
    }
  
  
  &.first {
    padding-top: 0px;
  }
  
  &.mobile {
    width: 100%;
    padding: 0;
  }
}

.footer-nav--sub-list {
  font-size: 15px;
  font-family: HelveticaNeueLTStd-Cn;
  text-transform: uppercase;
  
    @include at-query ($max, $medium) {
	  text-align: center;
    }
  
  &.last {
    margin-top: 10px;
  }
  .input-group-field {
    width: 277px;
    text-align: center;
  }
  &.mobile {
  	form {
      margin: auto;
    }
  }
}

/*============================================================================
  HOMEPAGE | index.html
==============================================================================*/

/*================= Homepage Banners | homepage--banners.liquid =======================*/

.overlay {
  position: relative;
  text-align: center;
  width: 100%;
  height: 100%;
}

/* Banner Full */

.banner-full {
    @include at-query ($max, $small){
      height: 200px;
      }
  
    @include at-query ($min, $postSmall){
      height: 300px;
      }
  
    @include at-query ($min, $large){
      height: 600px;
      }
}

.banner-full-text {
  text-align: center;
  color: white;
  
  @include at-query ($max, $medium){
    padding-top: 70px;
  }
  
  @include at-query ($min, $large){
    padding: 250px;
  }
}

.homepage--banner-full-image {
  height: 100%;
  background-size: cover;
  background-position: bottom;
  background-image: url({{'banner-full.jpg' | asset_url }});
}

/* Banner Halves */

.square {
  padding: 0;

    @include at-query ($max, $small){  
      height: 250px;
    }
  
    @include at-query ($min, $postSmall){  
      height: 450px;
    }  
  
    @include at-query ($min, $large){
      height: 675px;
    }
}

.square-text {
  color: white;
  text-align: center;
  
    @include at-query ($max, $small){
      padding-top: 175px;
      
      .btn--secondary {
    		padding: 10px 40px;
      }
    }
  
    @include at-query ($min, $postSmall){
      padding-top: 350px;
    }
  
    @include at-query ($min, $large){
      padding-top: 550px;
    }
}

.homepage--left-half {
  background-size: cover;
  background-position: bottom;
  height: 100%;
    
    @include at-query ($max, $large){
      background-image: url({{'banner-left-half.jpg' | asset_url }});
      }
  
    @include at-query ($min, $large){
      background-image: url({{'banner-left-half-mobile.jpg' | asset_url }});
      }
}

.homepage--right-half {
  background-size: cover;
  background-position: bottom;
  height: 100%;
    
    @include at-query ($max, $large){
      background-image: url({{'banner-right-half.jpg' | asset_url }});
      }
  
    @include at-query ($min, $large) {
      background-image: url({{'banner-right-half-mobile.jpg' | asset_url }});
      }
}

/* Banner Thirds */

.third {
  padding: 0;

    @include at-query ($max, $small){  
      height: 400px;
    }
  
    @include at-query ($min, $postSmall){  
      height: 500px;
    }  
  
    @include at-query ($min, $large){
      height: 525px;
    }
}

.third-text {
  color: white;
  text-align: center;
  
    @include at-query ($max, $small){
      padding-top: 400px;
      }
  
    @include at-query ($min, $postSmall){
      padding-top: 400px;
      }
  
    @include at-query ($min, $large){
      padding-top: 390px;
      } 
}

.homepage--left-third {
  background-size: cover;
  background-position: center;
  height: 100%;
    
    @include at-query ($max, $large){
      background-image: url({{'left-third-2.jpg' | asset_url }});
      }
  
    @include at-query ($min, $large){
      background-image: url({{'left-third-2.jpg' | asset_url }});
      }
}

.homepage--center-third {
  background-size: cover;
  background-position: center;
  height: 100%;
    
    @include at-query ($max, $large){
      background-image: url({{'banner-center-third.jpg' | asset_url }});
      }
  
    @include at-query ($min, $large){
      background-image: url({{'banner-center-third-mobile.jpg' | asset_url }});
      }
}

.homepage--right-third {
  background-size: cover;
  background-position: center;
  height: 100%;
    
    @include at-query ($max, $large){
      background-image: url({{'banner-right-third.jpg' | asset_url }});
      }
  
    @include at-query ($min, $large){
      background-image: url({{'banner-right-third-mobile.jpg' | asset_url }});
      }
}

/* Banner Text */

.banner-text {
  text-align: center;
  height: 231px;

  h2 {
    color: black;
  }

  .banner-text-padding {
    padding: 50px;
  }
}
  
.banner-text-logo {
  margin-bottom: 25px;
  width: 200px;
}

/*============================================================================
  COLLECTIONS
==============================================================================*/

/*===================== #Collection Overview Styling | collection.overview.liquid ============================*/

/* Banner Full */

.collection--banner-full-image {
  height: 100%;
}

.collection--banner-full {
  @include at-query ($max, $small){
    height: 200px;
    }
  
    @include at-query ($min, $postSmall){
      height: 300px;
    }
  
    @include at-query ($min, $large){
      height: 600px;
    }
}

.collection--banner-full-text {
  text-align: center;
  color: white;

  @include at-query ($max, $medium){
    padding-top: 70px;

    .btn--secondary.banner-button {
      padding: 10px 30px;
    }
  }
  
  @include at-query ($min, $large){
    padding-top: 250px;
  }  
}

/* Banner Halves */

.collection--banner-half-image {
  height: 100%;
}

.collection--banner-half-text {
  padding-top: 20px;  
  color: white;
  left: 0;
  opacity: 1;
  position: absolute;
  text-align: center;
  text-transform: uppercase;
  top: 40%;
  width: 100%;
}

/* Banner Thirds */

.collection--banner-third-image {
  height: 100%;  
}

.collection--banner-third-text {
  padding-top: 20px;  
  color: white;
  left: 0;
  opacity: 1;
  position: absolute;
  text-align: center;
  text-transform: uppercase;
  top: 40%;
  width: 100%;
}

/*====================== Standard Collections | collection.liquid =======================*/

/* Banner */

.collection--banner {
  height: 300px;
}

.collection--banner-image {
  height: 100%;
}

.collection--banner-text {
	top: 95px;  
	color: white;
	position: absolute;
}

.overlay {
	position: relative;
  text-align: center;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.3);
}

.collection--banner-title {
  width: 318px;
 	padding: 10px;
  border: solid white 2px;
 	 
  @include at-query ($max, $medium){ 
    margin: auto;
  }
}

/*====================== Collection Filters =================================*/

/* Filter Bar */

.collection--filter {
  border-top: 10px solid white;
  
  	ul{
      text-transform: uppercase;
      font-size: 16px;
  	}
    button {
    	float: right;
    	margin-top: -5px;
  }
}

.collection--filter-list {
  float: left;
  
  @include at-query ($max, $medium){
    font-size: 14px;
    
    a {
      padding-left: 0px;
    }
  }
}

li.collection--filter-list-sort {
  float: right;
  margin: -2px 0;
  padding-left: 20px;
  
  select#SortBy {
    border: none;
    padding: 0;
    text-transform: uppercase;
  }
}

.collection--filter-list-view {
  float: right;
}

.collection--filter-bar {
  cursor: default;
  height: 42px;
  background-color: lightgrey;
  margin: 0px 0px 10px;
  padding: 10px 20px;
  
    @include at-query ($max, $small){
      padding: 10px;
    }

  li {
    margin: 0;
    text-transform: uppercase;
  }

  > li {
    display: inline-block;
    text-align: left;    
  }
}

.collection--filter-bar-link {
  display: block;
  text-decoration: none;
  padding: 0 30px;
  white-space: nowrap;
      
  @include at-query ($max, $small){
      padding: 0 12px;
    }

  &:hover,
  &:active,
  &:focus {
    color: grey;
  }
}

/* Desktop Dropdowns */

.collection--filter-dropdown {
  display: none;
  position: absolute;
  margin: 0;
  z-index: $zindexNavDropdowns;
  background: lightgrey;
  padding: 5px 0 0 20px;
  width: 145px;

  .supports-no-touch .collection--filter-list:hover &,
  .collection--filter-list.collection--filter-bar-hover &,
  .collection--filter-bar-focus + & {
    display: block;
  }

  a {
    background-color: lightgrey;

  }
}

.collection--filter-dropdown {
  position: absolute;
}

.collection--filter-dropdown-list {
  font-size: 14px;
  text-transform: uppercase;
  padding: 0 0 10px;
  font-family: HelveticaNeueLTStd-Cn;
}

/* Mobile List */

li.collection--filter-list-mobile {
  float: left;
  display: inline-block;
  margin-top: -1px;
  
  select {
    border: none;
    width: auto;
    text-transform: uppercase;
    background: none;
    
    &.filter {
      padding-right: 10px;
    }
  }
}

/*======================== Collection Extras ==============================*/

.collection--icon {
	position: absolute;
    top: 0;
    right: 0;
    width: 30%;
    margin-right: 30px;
   @include at-query ($max, $medium) { 
     margin-right: 5px;
     top: -7px;
   }
}

/*============================================================================
  PRODUCTS
==============================================================================*/

/*====================== Breadcrumbs =======================*/

/* Standard Breadcrumb */

.breadcrumb {
  margin: 0px 0px 10px;
  padding: 10px 20px;
  background-color: lightgrey;
  text-transform: uppercase;
  letter-spacing: 1px;

  a {
    color: grey;
  }
  
  a,
  span {
    display: inline-block;

    &:first-child {
      padding-left: 0;
    }
  }
  .previous-product {
    float: right;
  }
}  

.message, .breadcrumb {
  font-family: $headerFontStack;
}

/* Breadcrum Highlight */

.breadcrumb-highlight {
  margin: 10px 0 3px;
  
  img {
    width: 100%;
  }
}

/*================== Product Primary Info =====================*/

/* Product Images */

.product-single__variants {
  display: none;

  .no-js & {
    display: block;
  }
}

.product-single__photos { 
  @include at-query ($max, $medium) {
    margin: 0;
  }
}

.product-single__photos,
.product-single__thumbnails {
  a, img {
    display: block;
    margin: 0 auto;
  }

  li {
  }
}

.grid-product__image-wrapper {
  position: relative;
}

.product-single__photos a, .product-single__photos img, .product-single__thumbnails a, .product-single__thumbnails .variant--images {
  margin:0;
}

ul#ProductThumbs {
    margin-top: 20px;
  
    @include at-query ($max, $medium){
      margin-top: 10px;
    }
}

/* Product Info */

.product--description {
    width: 380px;
  	margin-bottom: 20px;
  
    @include at-query ($max, $medium) {
      width: 88%;
    }
}

.product--info {
  margin-top: 20px;
  
  @include at-query ($max, $medium){
    margin-top: 0;
    margin-left: 20px;
  }
  
  h2 {
    margin-top: 10px;
    padding-bottom: 5px;
    
    @include at-query ($max, $small){ 
      width: 85%;
    }
  }
}

/*===================== Product Secondary Info ========================*/

/* NEED TO FIGURE OUT WHATS GOING ON HERE SEEMS LIKE A BUNCH OF EXTRA CRAP */

/* Product Secondary */

.product-secondary-box {
  width: 100%;
  
  @include at-query ($max, $medium) {
    height: auto;
  }
}

.product-secondary-left {
  height: 100%;
}

.product-secondary-left-images {
  height: 100%;
}

.product-secondary-main {
      height: 600px;
    width: 100%;
}

.product-secondary-main-large {
    height: 100%;
    border-left: 10px white solid;
	width: 100%;
}

.product-secondary-images {
	height: 100%;
  
  li {
    width: 100%;
    height: 50%;
    margin-bottom: 0;
  }
}

.product-secondary-image-1 {
  width: 100%;
  height: 100%;
  border-left: 10px solid white;
  border-bottom: 5px solid white;
}

.product-secondary-image-2 {
  width: 100%;
  height: 100%;
  border-left: 10px solid white;
  border-top: 5px solid white;
}

.product-secondary-box {
    @include at-query ($max, $medium) {
    	display: -webkit-box;
    	display: -moz-box;
    	display: -ms-flexbox;
    	display: -webkit-flex;
    	display: flex;
    	-webkit-box-orient: vertical;
    	-moz-box-orient: vertical;
    	-webkit-flex-direction: column;
    	-ms-flex-direction: column;
    	flex-direction: column;
    	/* optional */
    	-webkit-box-align: start;
    	-moz-box-align: start;
    	-ms-flex-align: start;
    	-webkit-align-items: flex-start;
    	align-items: flex-start;
    	padding-top: 10px;
  }

   .product-secondary-left {
    	-webkit-box-ordinal-group: 2;
    	-moz-box-ordinal-group: 2;
    	-ms-flex-order: 2;
    	-webkit-order: 2;
    	order: 2;
  }

  .product-tabs {
    -webkit-box-ordinal-group: 1;
    -moz-box-ordinal-group: 1;
    -ms-flex-order: 1;
    -webkit-order: 1;
    order: 1;
  }
}

.tabs--reviews {
  padding: 10px;
}

/*======================== Additional Product Info =========================*/

.products--related {
  margin-top: 20px;
  margin: 10px;
}
